# Отчёт: прототип переводчика pt-BR ⇄ ru (эмуляция POCO X7 Pro)

Профиль эмуляции `poco-cpu`: 4 потока CPU, лимит 8 ГБ RAM, int8/Q4 квантование.

## Результаты бенчмарков

### MT (текст → текст), 40 фраз

> **Исправлено 2026-09-10.** В `mt_bench.py` chrF считался с неверной формой ссылок
> (`[[ref] for …]` вместо `[refs]`), что давало фиктивные 100.0. Все `results/mt_*.json`
> пересчитаны; старые значения сохранены в поле `chrf_old_buggy`. Добавлен движок `opus`.
> Подробности: `results/device/2026-09-10-opus-mt-bench.md`.

| Движок | pt→ru chrF | ru→pt chrF | Задержка p50 | Офлайн |
|---|---|---|---|---|
| **OPUS-MT tc-big int8 + split** (прямая пара) | **67.3** | **65.3** | **0.15 с** | да |
| OPUS-MT int8 **ONNX на телефоне** (Dimensity 1100, жадный декод) | 65.6 | 62.9 | **0.21 / 0.17 с** | да |
| Bergamot/Mozilla base-memory, пивот через en (bergamot-translator) | 62.8 | 60.8 | 0.03 с | да |
| NLLB-200-600M int8 | 59.8 | 64.2 | 0.4–0.55 с | да |
| Gemma 3 4B Q4_K_M | 72.6 | 72.4 | 1.7 с (на телефоне измерено 7.1 с) | да |
| OpenRouter nemotron-3-super-120b :free | 83.1 | 83.4 | 2.7–2.9 с | нет |

Было до исправления: NLLB 66.7 / 49.5, Gemma 100 / 90.3, OpenRouter 65.3 / 100.

### ASR — ИЗМЕРЕНО НА ТЕЛЕФОНЕ 2026-09-10 (Dimensity 1100, реальные дикторы Tatoeba)

| Модель | pt-BR WER | ru WER | RTF pt / ru | Размер |
|---|---|---|---|---|
| parakeet-tdt-0.6b-v3 int8 (оба языка) | **2.6%** | **1.2%** | 0.20 / 0.27 | 670 МБ |
| fastconformer-pt transducer int8 | 5.3% | — | **0.07** | 135 МБ |
| zipformer-small-ru streaming int8 | — | 2.5% | — / 0.13 | 28 МБ |
| nemotron-3.5 streaming 0.6b int8 | 11.8% | 8.8% | 0.84 / 0.86 | 682 МБ |
| whisper-base int8 | 28.9% | 12.5% | 0.23 / 0.21 | 160 МБ |

Прежняя таблица (faster-whisper small/turbo на синтетике в эмуляции) снята: на реальной речи и
реальном ARM whisper-base даёт 28.9% WER по pt против 2.6% у parakeet при той же скорости.
Подробности и оговорки: `results/device/2026-09-10-asr-phone.md`.

### TTS — ИЗМЕРЕНО НА ТЕЛЕФОНЕ 2026-09-11 (Dimensity 1100)

| Голос | RTF | Синтез/фразу | ASR-WER (разборчивость) |
|---|---|---|---|
| ru dmitri, Piper medium **fp32** | 0.151 | 0.36 с | 3.7% |
| pt faber / cadu, Piper medium **fp32** | 0.151 / 0.177 | 0.28 / 0.39 с | 1.3% / 1.3% |
| те же голоса **int8** | 0.35–0.38 | 0.67–0.83 с | 2.6–6.2% |
| Supertonic-3 int8 (ru / pt) | 0.68 / 0.71 | 2.6 / 2.1 с | 7.4% / 6.5% |

Piper на ПК давал RTF 0.06; на телефоне fp32 — 0.15, и **int8 медленнее fp32 в 2.5 раза**.
VITS не масштабируется по потокам — одного ядра достаточно. `results/device/2026-09-11-tts-phone.md`.

### Сквозной пайплайн — ИЗМЕРЕНО НА ТЕЛЕФОНЕ 2026-09-11 (Dimensity 1100, тёплые процессы)

| Конфигурация | ASR | MT | TTS до 1-го чанка | **до первого звука** (мед / p95) |
|---|---|---|---|---|
| pt→ru: fastconformer + OPUS int8 + Piper dmitri (4 потока) | 150 мс | 206 мс | 186 мс | **543 / 857 мс** |
| pt→ru: parakeet + OPUS + Piper (1 поток) | 315 мс | 173 мс | 383 мс | 863 / 2466 мс |
| ru→pt: parakeet + OPUS + Piper faber (1 поток) | 366 мс | 212 мс | 606 мс | 1201 / 2411 мс |

Без VAD (+0.3–0.7 с) и транспорта до наушника (A2DP +0.15–0.25 с). Прежняя оценка «≈3 с» с ПК
снята. Подробности: `results/device/2026-09-11-e2e-phone.md`, код — `bench/e2e/`.

## Рекомендуемая конфигурация для POCO X7 Pro
- **ASR**: Whisper small int8 (~460 МБ). Dimensity 8400 ≈ 4 потока ПК или чуть медленнее → RTF ~0.7–1.0, приемлемо.
- **MT по умолчанию**: NLLB int8 (~600 МБ) — 0.5 с/фраза.
- **MT «качество»**: Gemma 3 4B Q4_K_M через llama.cpp Vulkan (Mali-G720) — на ПК 1.7 с, на телефоне ожидаемо 2–5 с; включается по желанию.
- **TTS**: Piper (2 голоса, ~130 МБ).
- **Итого на телефоне**: ~1.2–3.7 ГБ, офлайн, без интернета.
- **OpenRouter**: только fallback при наличии сети; free-модели имеют общий пул и частые 429 — retry уже реализован, платные заблокированы флагом `ALLOW_PAID=False`.

## APK (2026-09-12)

`apk/AgentTranslator-1.0.apk` (83 МБ) и облегчённая `apk/AgentTranslator-1.0-slim.apk` (21 МБ,
без llama.cpp — он занимает 228 МБ нативных библиотек из 285 ради необязательного контекстного
уточнителя). Модели заливаются отдельно: `bench/apk/push_models.sh`, ~2,9 ГБ.

Сквозной конвейер в одном процессе, push-to-talk в обе стороны и непрерывный VAD-режим.
До первого звука **540 мс** (p95 860), при попадании в словарь **213–417 мс**.

Путь версий:
- **0.2** — foreground-сервис, словарь фраз с самообучением и кэшем звука
- **0.3** — маскирование чисел и адресов, нормализация pt-BR, контекстный LLM-уточнитель
- **0.4** — словарь на 191 тыс. пар из Tatoeba, пофразовый поиск, три исправления нормализатора
  ([разбор](results/2026-09-11-tatoeba-phrasebook.md))
- **0.5** — parakeet как единственная ASR вместо двух моделей (WER вдвое ниже, пунктуация
  на обеих сторонах), отпечаток голоса с языком в профиле; горячие слова и шумоподавитель
  проверены и отклонены по замеру ([разбор](results/2026-09-12-asr-upgrade.md))
- **0.6** — список своих слов с фонетическим сопоставлением и практическая транскрипция имён
  ([разбор](results/2026-09-12-wordlist.md))
- **0.7–0.9** — транслитерация адресов, починка плейсхолдера маскирования, кнопка «получше»
  через бесплатные модели OpenRouter, инструментация непрерывного режима
  ([бэклог](results/2026-09-12-backlog.md), [соак](results/2026-09-12-screenoff-soak.md))
- **1.0** — проверка языка перед переводом: parakeet многоязычный и сам определял язык, из-за
  чего фоновая английская речь переводилась и произносилась вслух
  ([разбор](results/2026-09-12-langgate.md))

Сборка, что внутри и чего нет — `bench/apk/README.md`. Решения и обоснования — `PLAN.md`.

## Файлы
- `translator.py` — сквозной CLI (push-to-talk; `--wav` для теста)
- `mt_bench.py`, `asr_bench.py`, `tts_bench.py` — бенчи
- `poco_limits.py` — профиль эмуляции (`POCO_PROFILE=poco-gpu` для GPU-прогона)
- `data/test_set.json` — 40+40 фраз, `data/glossary.json` — глоссарий
- `results/` — все прогоны

## Следующие шаги
1. Протестировать medium ASR (компромисс small/turbo) и живую речь с микрофона
2. Kokoro-82M как более естественный TTS
3. Стриминговый ASR (sherpa-onnx) для снижения задержки до ~2 с
4. Перенос на телефон: sherpa-onnx (ASR+TTS) + CTranslate2 + llama.cpp Vulkan
